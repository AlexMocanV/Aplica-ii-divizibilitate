#include <bits/stdc++.h>

using namespace std;

int main()
{
int n,ok=1,i,d,nr=1;
    cin>>n;
    for(i=2;nr<=n;i++)
    {
    ok=1;
	    for(d=2;d*d<=i;d++)
    {
	    if(i%d==0)
        {
		    ok=0;
        }
    }
    if(ok==1)
    {
		cout<<i<<" ";
		nr++;
    }


    }
}
