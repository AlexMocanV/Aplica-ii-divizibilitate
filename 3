#include <bits/stdc++.h>

using namespace std;

int main()
{
int n,ok=1,i,d,nr=0,y;
    cin>>n;
    for(i=1;i<=n;i++)
    {
        cin>>y;
    ok=1;
	    for(d=2;d*d<=y;d++)
    {
	    if(y%d==0)
        {
		    ok=0;
        }
    }
    if(ok==1)
    {
		nr++;
    }


    }
    cout<<nr;
}
