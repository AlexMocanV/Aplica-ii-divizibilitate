#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n,i,d,s;
    cin>>n;
    for(i=2;i<=n;i++)
    {   
        s=1;
        for(d=2;d*d<i;d++)
        {
            if(i%d==0)
            {
                s+=d;
                s+=i/d;
            }
        }
        if(s==i)
        {
            cout<<i<<" ";
        }
    }
    
    
    
    return 0;
}
